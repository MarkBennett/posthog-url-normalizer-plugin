{
  "name": "posthog-url-normalizer-plugin",
  "private": false,
  "version": "0.0.1",
  "description": "A PostHog plugin to normalize the format of urls in your application allowing you to more easily compare them in insights.",
  "scripts": {
    "test": "jest ."
  },
  "dependencies": {
    "@posthog/plugin-scaffold": "^1.0.4",
    "jest": "^28.1.1"
  }
}
